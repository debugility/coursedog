{
  "name": "coursedog",
  "version": "1.0.0",
  "description": "coursdog-test-project",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "allure:generate": "npx allure-commandline generate allure-results --clean -o allure-report",
    "allure:open": "npx allure-commandline open allure-report",
    "test": "REPORTER=allure playwright test && npm run allure:generate",
    "test:headed": "REPORTER=allure playwright test --headed && npm run allure:generate",
    "test:e2e": "REPORTER=allure playwright test tests/e2e && npm run allure:generate",
    "test:regression": "REPORTER=allure playwright test tests/regression && npm run allure:generate",
    "test:ui": "REPORTER=allure playwright test --ui"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.13",
    "@faker-js/faker": "10.4.0",
    "@playwright/test": "1.59.1",
    "allure-playwright": "3.7.1",
    "dotenv": "17.4.2",
    "typescript": "6.0.3"
  }
}
